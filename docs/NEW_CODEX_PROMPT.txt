Read `HANDOFF.md`, `AGENTS.md`, `TODO.md`, and `docs/13.pdf` before editing anything.

Before making code changes:

1. Run `git status --short`.
2. Summarize what this project does.
3. Summarize what has already been completed.
4. Identify the current unresolved issue.
5. List the files you need to inspect before editing.

Important rules:

- The database name must remain `Personal_Finance`.
- Do not run destructive git commands unless explicitly asked.
- Prefer Docker for verification.
- After backend/database changes, run the smoke test if possible.
- Update `HANDOFF.md` before ending the session.

Current known next task:
Fix the delete-user rule so a newly created user with only an empty zero-balance initial bank account can be deleted by the smoke test, while users with real financial activity remain protected.
